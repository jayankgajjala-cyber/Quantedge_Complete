# ═══════════════════════════════════════════════════════════════════
#  Quantedge Trading System — Production Requirements
#  Python 3.11 (matches Dockerfile: FROM python:3.11-slim)
#
#  KEY PINS & REASONS
#  ──────────────────
#  bcrypt==3.2.2        passlib 1.7.4 reads bcrypt.__about__ at import
#                       time; bcrypt>=4.0 removed that attr → AttributeError.
#                       MUST stay at 3.x until passlib is replaced.
#
#  apscheduler==3.10.4  APScheduler 4.x is a full rewrite with breaking API.
#                       AsyncIOScheduler, add_job, add_listener are 3.x APIs.
#                       Do NOT upgrade to 4.x without a full scheduler rewrite.
#
#  pydantic==2.7.1      models/schemas.py uses @validator (v1 compat API).
#                       Pydantic v2 still supports it but emits DeprecationWarning.
# ═══════════════════════════════════════════════════════════════════

# ── Core Framework ───────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
pydantic-settings==2.3.4
python-multipart==0.0.9
aiofiles==23.2.1

# ── Database ─────────────────────────────────────────────────────
sqlalchemy==2.0.30
alembic==1.13.1
psycopg2-binary==2.9.9        # Production: PostgreSQL / Supabase driver

# ── Auth & Security ──────────────────────────────────────────────
passlib[bcrypt]==1.7.4
bcrypt==3.2.2
python-jose[cryptography]==3.3.0
PyJWT==2.8.0
httpx==0.27.0
python-dotenv==1.0.1

# ── Data & Numerics ──────────────────────────────────────────────
yfinance==0.2.54
pandas==2.2.2
numpy==1.26.4
scipy==1.13.0
scikit-learn==1.5.0
pyarrow==16.1.0

# ── Timezone Data ────────────────────────────────────────────────
tzdata==2024.1                # Required on slim images — no /usr/share/zoneinfo

# ── Web Scraping ─────────────────────────────────────────────────
requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.2.2
cloudscraper==1.2.71

# ── TradingView ──────────────────────────────────────────────────
tradingview-ta==3.3.0

# ── Scheduling ───────────────────────────────────────────────────
apscheduler==3.10.4

# ── Testing ──────────────────────────────────────────────────────
pytest==8.2.2
pytest-asyncio==0.23.7
